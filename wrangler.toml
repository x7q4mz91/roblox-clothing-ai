name = "roblox-clothing-ai"
compatibility_date = "2026-06-04"

# No secret values are stored here. Set OPENAI_API_KEY in Cloudflare Pages environment variables
# or export it locally before running `wrangler pages dev`.
